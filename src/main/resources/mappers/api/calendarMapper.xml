<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
     
<mapper namespace="com.spring.erp_ordit.dao.api.CalendarMapper">

	<!-- 캘린더 리스트 -->
	<select id="getAllEvents" resultType="com.spring.erp_ordit.dto.api.CalendarDTO">
		SELECT * FROM calendar_event
	</select>
</mapper>