<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
     
<mapper namespace="com.spring.erp_ordit.dao.basic.BasicClientMapper">

<!-- 거래처 목록 조회 -->
<select id="clientList" resultType="com.spring.erp_ordit.dto.basic.BasicClientDTO">
   SELECT * FROM client_tbl
    ORDER BY client_code
</select>

<!-- 거래처 등록 -->
<insert id="basicInsertClient" parameterType="com.spring.erp_ordit.dto.basic.BasicClientDTO">
	INSERT INTO client_tbl (
		client_name,
		c_ceo,
		c_biz_num,
		c_tel,
		c_email,
		c_zone_code,
		c_base_address,
		c_detail_address,
		c_type,
		c_industry,
		c_status,
		c_note,
		c_reg_date
	)
	VALUES (
		#{client_name},
		#{c_ceo},
		#{c_biz_num},
		#{c_tel},
		#{c_email},
		#{c_zone_code},
		#{c_base_address},
		#{c_detail_address},
		#{c_type},
		#{c_industry},
		#{c_status},
		#{c_note},
		NOW()
	)
</insert>

<!-- 거래저 상세보기 -->
<select id="basicClientDetail" resultType="com.spring.erp_ordit.dto.basic.BasicClientDTO">
   SELECT * FROM client_tbl
    WHERE client_code=#{client_code} 
</select>

<!-- 거래처 수정 -->
<update id="basicClientUpdate" parameterType="com.spring.erp_ordit.dto.basic.BasicClientDTO">
  UPDATE client_tbl
  SET
    client_name = #{client_name},
    c_ceo = #{c_ceo},
    c_biz_num = #{c_biz_num},
    c_tel = #{c_tel},
    c_email = #{c_email},
    c_zone_code = #{c_zone_code},
    c_base_address = #{c_base_address},
    c_detail_address = #{c_detail_address},
    c_type = #{c_type},
    c_industry = #{c_industry},
    c_status = #{c_status},
    c_note = #{c_note}
  WHERE client_code = #{client_code}
</update>

<!-- 거래처 삭제 -->
<delete id="basicClientDelete" parameterType="com.spring.erp_ordit.dto.basic.BasicClientDTO">
	DELETE FROM client_tbl
	 WHERE client_code=#{client_code}
</delete>

</mapper>
